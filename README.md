# SwiftCart
1) What is the difference between null and undefined?
   Ans: Null occurs when the user does not want to insert any value. Undefined occurs when the user declares a variable but forgets to assign a value then JS treats it's undefined.
2) What is the difference between == and ===?
  Ans:  == it checks only the value suppose 5=="5" in this case its true because we only check the value here.
        === its heck both value and type 5=="5" its wrong because one is string other one is number 
3)  What is the significance of async/await in fetching API data?
    Ans: when we fetch data through api it can take long time to give a response . Async/Await allows user to fetch data without reloading the page that mean it doesnt contain so many time.
4) Explain the concept of Scope in JavaScript (Global, Function, Block).
   Ans: Global: Variable decleared outside of any function
         Function: Variablke decleared inside the function
         Block: Variable fecleared in the block its a thing from ES6
2) What is the use of the map() function in JavaScript? How is it different from forEach()?
   Ans:Map is used for creating a new array
      Foreach perfomes an action on all item in a list
